---
import MainLayout from '../../layouts/MainLayout.astro';
import LabPage from '../../pages/_LabPage';

const { labId } = Astro.params;
---

<MainLayout title={`Lab - ${labId}`}>
	<LabPage client:load labId={labId} />
</MainLayout>
